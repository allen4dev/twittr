import { NAME } from './constants';

export const SET_CURRENT_USER = `${NAME}/SET_CURRENT_USER`;
export const SET_TOKEN = `${NAME}/SET_TOKEN`;

export const ADD_USERS = `${NAME}/ADD_USERS`;

export const ADD_TWEETS = `${NAME}/ADD_TWEETS`;
export const ADD_TIMELINE_TWEETS = `${NAME}/ADD_TIMELINE_TWEETS`;

export const FOLLOW_USER = `${NAME}/FOLLOW_USER`;
