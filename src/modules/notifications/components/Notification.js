import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.li``;

const Notification = () => {
  return <Wrapper>Render a notification here</Wrapper>;
};

export default Notification;
